{
  "name": "sagsc-toolbox0.0.2",
  "version": "0.0.2",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix web\" \"npm run tauri dev\"",
    "build": "npm run build --prefix web && npm run tauri build",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.0",
    "@tauri-apps/cli": "^2.1.0",
    "concurrently": "^7.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "xlsx": "^0.18.5"
  }
}
